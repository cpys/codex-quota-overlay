{
  "name": "codex-quota-overlay-telemetry",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute codex-quota-overlay-telemetry --local --file schema.sql",
    "db:remote": "wrangler d1 execute codex-quota-overlay-telemetry --remote --file schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
