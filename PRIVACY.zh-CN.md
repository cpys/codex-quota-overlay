# 隐私说明

最后更新：2026-08-09

Codex 额度悬浮层以本地运行为首要原则。读取额度、寻找 Codex 窗口、定位悬浮层和写入运行日志，都在用户自己的 Windows 电脑上完成。

## Codex 数据

程序启动本机已经安装的 `codex.exe app-server`，调用 `account/rateLimits/read`。返回的额度百分比、重置时间和可选重置卡信息仅用于绘制悬浮层，不会发送给本项目维护者。

程序不会收集或上传 ChatGPT 账户资料、访问令牌、API Key、会话内容、会话标题、提示词、回复、源代码、文件路径、屏幕截图或浏览器 Cookie。

## 可选匿名使用统计

源码构建默认没有统计服务地址，因此统计不可用。配置了 HTTPS 统计地址的版本，会先征得用户同意；之后可以随时从托盘菜单更改。

启用后，程序每 24 小时最多发送一次 `daily_active` 心跳，只包含：

- 本程序随机生成的安装 UUID；
- 悬浮层版本号；
- 固定的平台值 `windows`；
- 操作系统报告的 Windows 版本；
- 当前界面语言；
- 事件时间和数据结构版本。

应用负载不会主动包含 IP。与任何 HTTPS 请求一样，托管和网络服务为了传输请求会处理来源 IP，安全日志中也可能短暂出现。项目的统计数据库不得把 IP 当作用户标识，也不得把它复制到统计记录。

随机安装 UUID 只用于估算活跃安装数，保存在 `%LOCALAPPDATA%\CodexQuotaOverlay\settings.json`。删除这个文件后，下次启动会生成新标识。参考统计后端最多保留 90 天的心跳记录。

## 本地文件

- `%LOCALAPPDATA%\CodexQuotaOverlay\settings.json`：随机安装 ID、统计选择和上次成功心跳时间。
- `%LOCALAPPDATA%\CodexQuotaOverlay\overlay.log`：简短的运行错误摘要。

卸载程序不会自动删除这些文件。退出应用后可以手动清理。

## 变更和问题

重要隐私变更会同时记录在更新日志和本文档中。如有问题，可以提交 GitHub Issue，但请勿公开账户资料、令牌、包含敏感信息的日志或安装 UUID。
